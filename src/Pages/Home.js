import React from 'react';
import Spinner from '../components/spinner/Spinner';

const Home = () => {
    return (
        <div>
            <Spinner />

        </div>
    );
};

export default Home;